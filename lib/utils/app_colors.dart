import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF695CFF);
  static const Color primaryLight = Color(0xFFF2F0FC);

  // Text colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF4B4B4B);
  static const Color textHint = Color(0xFFABAFB1);
  static const Color textError = Color(0xFFD22121);

  // Border colors
  static const Color border = Color(0xFFABAFB1);
  static const Color borderLight = Color(0xFFD2D2D2);
  static const Color divider = Color(0xFFF2F2F2);

  // Background colors
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color overlay = Color(0x80000000); // 50% black overlay
}
